#ifndef DEFINES_H
#define DEFINES_H

#include <iostream>
#include <fstream>
#include <string>
#include <sstream>

using namespace std;

bool checker(const uint8_t* board, const uint8_t* refBoard, bool acceptZero);

#endif
